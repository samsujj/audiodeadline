import { SerchbyrolePipe } from './serchbyrole.pipe';

describe('SerchbyrolePipe', () => {
  it('create an instance', () => {
    const pipe = new SerchbyrolePipe();
    expect(pipe).toBeTruthy();
  });
});
