import { Commission3Pipe } from './commission3.pipe';

describe('Commission3Pipe', () => {
  it('create an instance', () => {
    const pipe = new Commission3Pipe();
    expect(pipe).toBeTruthy();
  });
});
