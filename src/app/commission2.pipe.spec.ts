import { Commission2Pipe } from './commission2.pipe';

describe('Commission2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Commission2Pipe();
    expect(pipe).toBeTruthy();
  });
});
