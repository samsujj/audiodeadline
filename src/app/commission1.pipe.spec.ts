import { Commission1Pipe } from './commission1.pipe';

describe('Commission1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Commission1Pipe();
    expect(pipe).toBeTruthy();
  });
});
